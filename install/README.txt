Installation

This module requires the core JFrame database tables found in vendor/jframe/install.

1. Create the users table with the users.sql script in this directory.

2. Move user.config.php to your config directory